/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!******************************************************************************!*\
  !*** ./assets/js/frontend/elementor/widgets/woo-products/products-swiper.js ***!
  \******************************************************************************/
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
/**
 * Woocommerce Product Swiper JS.
 *
 * @since 1.0.0
 * @version 1.0.0
 */

(function ($) {
  'use strict';

  // eslint-disable-next-line no-shadow
  var RbbWooProductsSwiperHandler = function RbbWooProductsSwiperHandler($scope, $) {
    var swiper;
    var $element = $($scope).find('.rbb-swiper');
    if ($element.length) {
      var $config = $element.data('swiper') || {};
      // eslint-disable-next-line no-undef
      swiper = new Swiper($element.get(0), $config);
      var $countdown = $($scope).find('[data-countdown-date]');
      if ($countdown.length && (typeof RbbCountdown === "undefined" ? "undefined" : _typeof(RbbCountdown)) === 'object') {
        // eslint-disable-next-line no-undef
        RbbCountdown.render($countdown);
      }
      var $select = $element.closest('.rbb_woo_products').find('select');
      // eslint-disable-next-line no-undef
      RbbSelectAjax.render($select, function (data) {
        var html = $($.parseHTML(data));
        var node = [];
        $.each(html, function (i, el) {
          if ($(el).hasClass('item')) {
            node[i] = "<div class='swiper-slide'>" + el.outerHTML + '</div>';
          }
        });
        $element.find('.swiper-wrapper').html(node.join(''));
        swiper.update();
      }, function () {
        $(document.body).triggerHandler('rbb-select-ajax-before-send', $element);
      }, function () {
        $(document.body).triggerHandler('rbb-select-ajax-complete', $element);
      });
    }
  };
  $(window).on('elementor/frontend/init', function () {
    elementorFrontend.hooks.addAction('frontend/element_ready/rbb_woo_products.default', RbbWooProductsSwiperHandler);
  });
})(jQuery);
/******/ })()
;
//# sourceMappingURL=products-swiper.js.map