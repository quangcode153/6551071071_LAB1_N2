/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
/*!************************************************!*\
  !*** ./assets/js/frontend/components/modal.js ***!
  \************************************************/
/**
 * Modal.
 *
 * @version 1.0.0
 * @since 1.0.0
 */



function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var RisingBambooModal = /*#__PURE__*/function () {
  function RisingBambooModal() {
    _classCallCheck(this, RisingBambooModal);
  }
  _createClass(RisingBambooModal, [{
    key: "modal",
    value: function modal(modalId, event) {
      if (event) {
        event.preventDefault();
      }
      var $this = this;
      var $showClass = 'show';
      var modal = jQuery(modalId);
      if (!modal.hasClass('rbb-modal')) {
        modal.addClass('rbb-modal');
      }
      if (!modal.children('.rbb-modal-dialog').length) {
        modal.children().wrapAll('<div class="rbb-modal-dialog"><div class="rbb-modal-body"></div></div>');
      }
      if (!modal.find('.rbb-close-modal').length) {
        modal.children('.rbb-modal-dialog').prepend('<header class="rbb-modal-header"><button class="rbb-close-modal" aria-label="close modal" tabindex="0">✕</button></header>');
      }
      var close = modal.find('.rbb-close-modal');
      if (!modal.children('.rbb-modal-backdrop').length) {
        modal.prepend('<div class="rbb-modal-backdrop"></div>');
      }
      jQuery('body').children(modalId).remove();
      modal.appendTo('body');
      modal.addClass($showClass);
      close.on('click', function (e) {
        e.preventDefault();
        jQuery(this).closest(modalId).removeClass($showClass);
        $this.stopPlayer(jQuery(this).closest(modalId));
      });
      jQuery('.rbb-modal-backdrop').on('click', function () {
        jQuery(this).closest('.rbb-modal').removeClass($showClass);
        $this.stopPlayer(jQuery(this).closest('.rbb-modal'));
      });
      jQuery(document).on('keyup', function (e) {
        if (e.key === 'Escape' && jQuery(document).find('.rbb-modal.' + $showClass)) {
          jQuery(document).find('.rbb-modal.' + $showClass).removeClass($showClass);
          $this.stopPlayer(jQuery(document).find('.rbb-modal.' + $showClass));
        }
      });
    }
  }, {
    key: "stopPlayer",
    value: function stopPlayer(modal) {
      var iframe = modal.find('iframe');
      if (iframe.length) {
        iframe.attr('src', iframe.attr('src'));
      } else if (modal.find('video')) {
        var video = modal.find('video');
        if (video.length) {
          video[0].pause();
        }
      }
    }
  }]);
  return RisingBambooModal;
}();
window.RisingBambooModal = new RisingBambooModal();
/******/ })()
;
//# sourceMappingURL=modal.js.map