/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
/*!**************************************************************!*\
  !*** ./assets/js/frontend/components/woocommerce-filters.js ***!
  \**************************************************************/
/**
 * Filters
 *
 * @package
 * @version 1.0.0
 * @since 1.0.0
 */



function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var RbbWoocommerceFilters = /*#__PURE__*/function () {
  function RbbWoocommerceFilters() {
    _classCallCheck(this, RbbWoocommerceFilters);
  }
  _createClass(RbbWoocommerceFilters, [{
    key: "filterByUrl",
    value: function filterByUrl($url) {
      var $loadMore = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      $url = decodeURIComponent($url);
      if (!$loadMore) {
        history.pushState({}, null, $url);
      }
      jQuery(document.body).trigger('rbb_product_loading');
      jQuery.ajax({
        url: $url,
        type: 'GET',
        dataType: 'html',
        success: function success(response) {
          var $response = jQuery(response);
          var $wrapperEl = '.rbb-product-catalog';
          var $wrapper = jQuery($wrapperEl);
          if (!$loadMore) {
            $wrapper.find('.rbb_woo_products .item').remove();
          }
          var $newItems = $response.find($wrapperEl + ' .rbb_woo_products').html();
          if ($loadMore) {
            $wrapper.find('.rbb_woo_products').append($newItems);
          } else {
            $wrapper.find('.rbb_woo_products').html($newItems);
          }
          jQuery(document.body).trigger('rbb_product_products_loaded', $wrapper);
          var fragments = ['.woocommerce-result-count', '.woocommerce-pagination', '#rbb-active-filters-bar'];
          var $widgets = jQuery('.sidebar-shop-filter-widget');
          $widgets.each(function () {
            var id = jQuery(this).attr('id');
            fragments.push('#' + id);
          });
          var $totalFragments = fragments.length;
          for (var i = 0; i < $totalFragments; i++) {
            var key = fragments[i],
              $key = jQuery(key);
            if ($key.length > 0) {
              $key.empty();
              var $newElement = $response.find(key);
              if ($newElement.length > 0) {
                $key.html($newElement.html());
                jQuery(document.body).trigger('rbb_product_fragment_loaded', $key);
              }
            }
          }
          jQuery(document.body).trigger('rbb_product_fragments_loaded');
        },
        beforeSend: function beforeSend() {
          if ($loadMore) {
            jQuery('.rbb-load-more').addClass('loading');
          }
          jQuery(document.body).trigger('rbb-woocommerce-filter-before-send', $url);
        },
        complete: function complete() {
          if ($loadMore) {
            jQuery('.rbb-load-more').removeClass('loading');
          }
          jQuery(document.body).trigger('rbb-woocommerce-filter-complete', $url);
        }
      });
    }
  }]);
  return RbbWoocommerceFilters;
}();
window.RbbWoocommerceFilters = new RbbWoocommerceFilters();
/******/ })()
;
//# sourceMappingURL=woocommerce-filters.js.map