/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
var __webpack_exports__ = {};
/*!***************************************************!*\
  !*** ./assets/js/frontend/components/swatches.js ***!
  \***************************************************/
/**
 * Swatches
 *
 * @version 1.0.0
 * @since 1.0.0
 */



function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
var RbbSwatches = /*#__PURE__*/function () {
  function RbbSwatches() {
    _classCallCheck(this, RbbSwatches);
    var self = this;
    self.init();
    // Make sure work when ajax load ( Quick view )
    jQuery(document).change(function () {
      self.init();
    });
  }
  _createClass(RbbSwatches, [{
    key: "init",
    value: function init() {
      this.$form = jQuery('form.rbb-swatches.variations_form');
      this.register();
      this.ajaxAddToCart();
    }
  }, {
    key: "register",
    value: function register() {
      var self = this,
        $term = self.$form.find('.rbb-swatch__term'),
        $activeTerm = self.$form.find('.rbb-swatch__term:not(.rbb-swatch__disabled)');
      self.$form.find('select').each(function () {
        self.updateLabel(this);
      });
      $term.each(function () {
        var $this = jQuery(this),
          term = $this.data('term'),
          title = $this.attr('title'),
          attr = $this.parent().data('attribute'),
          $selectBox = self.$form.find('select#' + attr),
          val = $selectBox.val();
        if (val !== '' && term === val) {
          jQuery(this).addClass('rbb-swatch__selected');
          jQuery('body').trigger('rbb-swatch__selected_event', [attr, term, title, $this]);
        }
        jQuery($selectBox).on('change', function () {
          var $availableAttr = jQuery(this).find('option')
          // eslint-disable-next-line array-callback-return
          .map(function () {
            if (this.value !== '') {
              return this.value;
            }
          }).get();
          if (!$availableAttr.includes(term)) {
            jQuery(this).addClass('rbb-swatch__disabled');
          }
        });
      });
      $activeTerm.off('click').on('click', function (e) {
        var $this = jQuery(this);
        if ($this.hasClass('rbb-swatch__disabled')) {
          return false;
        }
        var term = $this.data('term'),
          title = $this.attr('title'),
          attr = $this.parent().data('attribute'),
          $selectBox = self.$form.find('select#' + attr);
        $selectBox.val(term).trigger('change');
        $this.parent('.rbb-swatch').find('.rbb-swatch__selected').removeClass('rbb-swatch__selected');
        $this.addClass('rbb-swatch__selected');
        jQuery('body').trigger('rbb-swatch__selected_event', [attr, term, title, $this]);
        e.preventDefault();
      });
      self.$form.on('woocommerce_update_variation_values', function () {
        self.$form.find('select').each(function () {
          var $this = jQuery(this);
          var $swatch = $this.parent().find('.rbb-swatch');
          $swatch.find('.rbb-swatch__term').removeClass(['rbb-swatch__enabled', 'cursor-pointer']).addClass(['rbb-swatch__disabled', 'cursor-not-allowed']);
          $this.find('option.enabled').each(function () {
            var val = jQuery(this).val();
            $swatch.find('.rbb-swatch__term[data-term="' + val + '"]').removeClass(['rbb-swatch__disabled', 'cursor-not-allowed']).addClass(['rbb-swatch__enabled', 'cursor-pointer']);
          });
          // Update label
          $this.on('change', function () {
            self.updateLabel(this);
          });
        });
      });
      self.$form.on('reset_data', function () {
        jQuery('body').trigger('rbb-swatches_reset');
        jQuery(this).find('.rbb-swatch__selected').removeClass('rbb-swatch__selected');
        jQuery(this).find('select').each(function () {
          var attr = jQuery(this).attr('id');
          var title = jQuery(this).find('option:selected').text();
          var term = jQuery(this).val();
          if (term !== '') {
            jQuery(this).parent().find('.rbb-swatch__term[data-term="' + term + '"]').addClass('rbb-swatch__selected');
            jQuery('body').trigger('rbb-swatches_reset-attr', [attr, term, title]);
          }
        });
      });
      self.$form.on('show_variation', function () {
        self.$form.find('select').each(function () {
          var val = jQuery(this).val(),
            attr = jQuery(this).data('attribute_name');
          self.$form.find('.ajax_add_to_cart').attr('data-' + attr, val).data(attr, val);
        });
      });
    }
  }, {
    key: "updateLabel",
    value: function updateLabel($selectEl) {
      var $selectText = '';
      if (jQuery($selectEl).val()) {
        $selectText = jQuery($selectEl).find('option:selected').text();
      }
      jQuery($selectEl).closest('.rbb-swatch__wrap').find('.label label .label-val').html($selectText);
    }
  }, {
    key: "ajaxAddToCart",
    value: function ajaxAddToCart() {
      this.updateVariationData();
      jQuery(document.body).on('should_send_ajax_request.adding_to_cart', function (event, button) {
        return !(jQuery(button).hasClass('wc-variation-selection-needed') || jQuery(button).hasClass('disabled'));
      });
    }
  }, {
    key: "updateVariationData",
    value: function updateVariationData() {
      jQuery('input.variation_id').on('change', function () {
        var $variationId = jQuery(this).val(),
          $addToCartBtn = jQuery(this).closest('form.cart').find('.ajax_add_to_cart');
        if ($variationId.length) {
          $addToCartBtn.attr('data-product_id', $variationId);
        } else {
          var $productId = jQuery(this).closest('form.cart').find('input[name="product_id"]').val();
          if ($productId.length) {
            $addToCartBtn.attr('data-product_id', $productId);
          }
          jQuery.each($addToCartBtn.data(), function (attr) {
            if (attr.includes('attribute_pa')) {
              $addToCartBtn.removeData(attr).removeAttr('data-' + attr);
            }
          });
        }
      });
    }
  }]);
  return RbbSwatches;
}();
window.RbbSwatches = new RbbSwatches();
/******/ })()
;
//# sourceMappingURL=swatches.js.map